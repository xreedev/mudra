/**
 * Typography is deliberately local to the speech test bed.  The production app must not depend
 * on this until a bundled font and a native microphone implementation are explicitly approved.
 */
export const speechTestFont = 'MudraSpeechTestSans';
