<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">

      <!-- GPU offload via llama.rn was tried and reverted: on this device, the
           only GPU-capable prebuilt library bundles OpenCL together with
           Hexagon NPU support, and this OEM (vivo/iQOO) blocks unsigned/
           third-party FastRPC access to the Hexagon DSP (fails with
           "remote_session_control ... user err 0x73"). That failure surfaces
           as a non-std::exception across the JSI boundary ("Unknown error"),
           and once that combined library is loaded into the process, even a
           CPU-only retry (n_gpu_layers=0) fails identically — there is no
           OpenCL-only variant to fall back to. CPU-only already clears the
           <1.5s budget at ~12 tok/s, so we stick with it. See
           llm-testbed-decisions.md memory for the full investigation. -->
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
