{
  "name": "@mudra/sign-embeddings",
  "version": "0.0.1",
  "private": true,
  "description": "Portable custom-sign-via-embedding logic (matching, prototypes, enrollment) for MUDRA+. No RN/native deps — ports into app/src/features/signing/ unchanged. See INTEGRATION.md.",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  }
}
